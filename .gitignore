# dependencies
node_modules/
.pnp
.pnp.js

# next
.next/
out/
build/
dist/

# typescript
*.tsbuildinfo
next-env.d.ts

# env
.env
.env.local
.env.*.local
!.env.example

# data (SQLite + 임시 오디오 + 모델 — 개인 자료와 671MB 짜리 가중치)
data/
*.db
*.db-journal
*.db-shm
*.db-wal
# 주의: drizzle/ (마이그레이션 SQL)는 커밋해야 함 — Docker 자동 마이그레이션에 필요

# 전사 모델. 볼륨에 산다 (`npm run model:fetch`).
*.onnx
*.tar.bz2

# logs
*.log
npm-debug.log*

# os
.DS_Store
Thumbs.db
